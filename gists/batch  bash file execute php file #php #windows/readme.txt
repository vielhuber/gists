# first add the script folder to your PATH
export PATH=~/.composer/vendor/bin:$PATH (unix)
Systemsteuerung > System > Erweiterte Systemeinstellungen > Umgebungsvariablen... > Systemvariablen > Path (windows)

# run on windows
script

# run on unix
script